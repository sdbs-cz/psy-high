%h1.se-header= t('lineup.header')

//%p.sched
//  = link_to t('schedule.link'), '/program.pdf', class: 'sched-l'

.lup-wr--headliners<>
  = partial('partials/band', collection: lineup_data(:headliners), locals: {arity: 2, type: 'detail'})

.lup-wr--major<>
  = partial('partials/band', collection: lineup_data(:support), locals: {arity: 3, type: 'link'})
